on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: docker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - run: go build -o app .
      - run: docker login forgejo-http:3000 -u whunter -p ${{ secrets.REGISTRY_TOKEN }
      - run: docker build -t forgejo-http:3000/YOUR_USER/hashecho:latest .
      - run: docker push forgejo-http:3000/YOUR_USER/hashecho:latest
